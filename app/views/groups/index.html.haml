%h4 Refresh to generate a new list of groups!

- @groups.each_with_index do |group, index|
  .group.pull-left
    %h3= "Group #{index + 1}"
    %ul
      - group.each do |team_member|
        %li= team_member.full_name
